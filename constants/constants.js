const defaultColors = require('./colors/default');
const sizes = require('./scalars/sizes');
const flexbox = require('./css/flexbox');

module.exports = {
    colors: {
        default: defaultColors
    },
    scalars: {
        sizes: sizes
    },
    css: {
        flexbox: flexbox
    }
};